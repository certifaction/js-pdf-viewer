{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "lint": "lerna run lint",
        "build": "lerna run build",
        "create-version": "npm run build && lerna version --no-git-tag-version --no-push && npm install",
        "force-create-version": "npm run build && lerna version --force-publish --no-git-tag-version --no-push && npm install"
    },
    "devDependencies": {
        "lerna": "^7.1.4"
    },
    "volta": {
        "node": "20.5.0"
    }
}
