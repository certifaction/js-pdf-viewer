{
    "name": "@certifaction/js-pdf-viewer",
    "private": true,
    "type": "module",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/certifaction/js-pdf-viewer.git"
    },
    "publishConfig": {
        "registry": "https://npm.pkg.github.com"
    },
    "scripts": {
        "lint:oxlint": "oxlint .",
        "lint:eslint": "eslint . --cache",
        "lint:stylelint": "stylelint \"**/*.{scss,vue}\"",
        "lint": "run-s lint:*",
        "type-check": "lerna run type-check",
        "create-version": "lerna version --no-git-tag-version --no-push && pnpm install",
        "force-create-version": "lerna version --force-publish --no-git-tag-version --no-push && pnpm install"
    },
    "devDependencies": {
        "@certifaction/vue3-webapp-config": "^19.0.1",
        "lerna": "^9.0.7",
        "npm-run-all2": "^9.0.2"
    }
}
